<?xml version="1.0" encoding="UTF-8"?>
<queries>
    <query name="user.insert"><![CDATA[
        INSERT INTO users (id, clerk_id, email, name, created_at, updated_at)
        VALUES (:id, :clerkId, :email, :name, :createdAt, :updatedAt)
    ]]></query>

    <query name="user.findByClerkId"><![CDATA[
        SELECT id, clerk_id, email, name, created_at, updated_at
        FROM users
        WHERE clerk_id = :clerkId
        LIMIT 1
    ]]></query>

    <query name="user.findById"><![CDATA[
        SELECT id, clerk_id, email, name, created_at, updated_at
        FROM users
        WHERE id = :id
        LIMIT 1
    ]]></query>

    <query name="user.updateProfile"><![CDATA[
        UPDATE users
        SET email = :email, name = :name, updated_at = :updatedAt
        WHERE clerk_id = :clerkId
    ]]></query>

    <query name="user.anonymize"><![CDATA[
        UPDATE users
        SET email      = CONCAT('deleted-', id, '@deleted.invalid'),
            name       = 'Usuário Removido',
            updated_at = :updatedAt
        WHERE clerk_id = :clerkId
    ]]></query>
</queries>
