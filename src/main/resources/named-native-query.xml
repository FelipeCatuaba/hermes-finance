<?xml version="1.0" encoding="UTF-8"?>
<queries>
    <query name="user.insert"><![CDATA[
        INSERT INTO users (id, external_auth_id, email, name, password_hash, role, active, created_at, updated_at)
        VALUES (:id, :externalAuthId, :email, :name, :passwordHash, :role, :active, :createdAt, :updatedAt)
    ]]></query>

    <query name="user.findByExternalAuthId"><![CDATA[
        SELECT id, external_auth_id, email, name, password_hash, role, active, created_at, updated_at
        FROM users
        WHERE external_auth_id = :externalAuthId
        LIMIT 1
    ]]></query>

    <query name="user.findById"><![CDATA[
        SELECT id, external_auth_id, email, name, password_hash, role, active, created_at, updated_at
        FROM users
        WHERE id = :id
        LIMIT 1
    ]]></query>

    <query name="user.findByEmail"><![CDATA[
        SELECT id, external_auth_id, email, name, password_hash, role, active, created_at, updated_at
        FROM users
        WHERE lower(email) = lower(:email)
        LIMIT 1
    ]]></query>

    <query name="user.updateProfile"><![CDATA[
        UPDATE users
        SET email      = :email,
            name       = :name,
            updated_at = :updatedAt
        WHERE external_auth_id = :externalAuthId
    ]]></query>

    <query name="user.updateInternalCredentials"><![CDATA[
        UPDATE users
        SET name = :name,
            password_hash = :passwordHash,
            role = :role,
            active = :active,
            updated_at = :updatedAt
        WHERE id = :id
    ]]></query>

    <query name="user.anonymize"><![CDATA[
        UPDATE users
        SET email      = CONCAT('deleted-', id, '@deleted.invalid'),
            name       = 'Usuário Removido',
            updated_at = :updatedAt
        WHERE external_auth_id = :externalAuthId
    ]]></query>

    <query name="refreshToken.insert"><![CDATA[
        INSERT INTO refresh_tokens (id, user_id, token_hash, expires_at, revoked_at, created_at)
        VALUES (:id, :userId, :tokenHash, :expiresAt, :revokedAt, :createdAt)
    ]]></query>

    <query name="refreshToken.findByHash"><![CDATA[
        SELECT id, user_id, token_hash, expires_at, revoked_at, created_at
        FROM refresh_tokens
        WHERE token_hash = :tokenHash
        LIMIT 1
    ]]></query>

    <query name="refreshToken.revoke"><![CDATA[
        UPDATE refresh_tokens
        SET revoked_at = :revokedAt
        WHERE token_hash = :tokenHash
          AND revoked_at IS NULL
    ]]></query>

    <query name="refreshToken.revokeAllForUser"><![CDATA[
        UPDATE refresh_tokens
        SET revoked_at = :revokedAt
        WHERE user_id = :userId
          AND revoked_at IS NULL
    ]]></query>

    <query name="familyMember.insert"><![CDATA[
        INSERT INTO family_members (id, user_id, name, relation, active, created_at)
        VALUES (:id, :userId, :name, :relation, :active, :createdAt)
    ]]></query>

    <query name="familyMember.findByUserId"><![CDATA[
        SELECT id, user_id, name, relation, active, created_at
        FROM family_members
        WHERE user_id = :userId
          AND (:includeInactive = true OR active = true)
        ORDER BY created_at DESC
    ]]></query>

    <query name="familyMember.findById"><![CDATA[
        SELECT id, user_id, name, relation, active, created_at
        FROM family_members
        WHERE id = :id
        LIMIT 1
    ]]></query>

    <query name="familyMember.update"><![CDATA[
        UPDATE family_members
        SET name = :name,
            relation = :relation,
            active = :active
        WHERE id = :id
    ]]></query>

    <query name="familyMember.deactivate"><![CDATA[
        UPDATE family_members
        SET active = false
        WHERE id = :id
    ]]></query>

    <query name="expense.insert"><![CDATA[
        INSERT INTO expenses (
            id,
            user_id,
            family_member_id,
            category_id,
            installment_group_id,
            description,
            amount,
            expense_date,
            installment_number,
            total_installments,
            is_recurring,
            is_fixed,
            payment_method,
            notes,
            scope,
            created_at,
            updated_at
        )
        VALUES (
            :id,
            :userId,
            :familyMemberId,
            :categoryId,
            :installmentGroupId,
            :description,
            :amount,
            :expenseDate,
            :installmentNumber,
            :totalInstallments,
            :isRecurring,
            :isFixed,
            :paymentMethod,
            :notes,
            :scope,
            :createdAt,
            :updatedAt
        )
        RETURNING id,
                  user_id,
                  family_member_id,
                  category_id,
                  installment_group_id,
                  description,
                  amount,
                  expense_date,
                  installment_number,
                  total_installments,
                  is_recurring,
                  is_fixed,
                  payment_method,
                  notes,
                  scope,
                  created_at,
                  updated_at
    ]]></query>

    <query name="expense.findById"><![CDATA[
        SELECT id,
               user_id,
               family_member_id,
               category_id,
               installment_group_id,
               description,
               amount,
               expense_date,
               installment_number,
               total_installments,
               is_recurring,
               is_fixed,
               payment_method,
               notes,
               scope,
               created_at,
               updated_at
        FROM expenses
        WHERE id = :id
        LIMIT 1
    ]]></query>

    <query name="expense.findByUserAndPeriod"><![CDATA[
        SELECT e.id,
               e.user_id,
               e.family_member_id,
               e.category_id,
               e.installment_group_id,
               e.description,
               e.amount,
               e.expense_date,
               e.installment_number,
               e.total_installments,
               e.is_recurring,
               e.is_fixed,
               e.payment_method,
               e.notes,
               e.scope,
               e.created_at,
               e.updated_at,
               c.name AS category_name,
               c.icon AS category_icon,
               c.color_hex AS category_color_hex,
               fm.name AS family_member_name,
               fm.relation AS family_member_relation
        FROM expenses e
        LEFT JOIN expense_categories c ON c.id = e.category_id
        LEFT JOIN family_members fm ON fm.id = e.family_member_id
        WHERE e.user_id = :userId
          AND e.expense_date >= :startDate
          AND e.expense_date < :endDate
          AND (:categoryId IS NULL OR e.category_id = :categoryId)
          AND (:familyMemberId IS NULL OR e.family_member_id = :familyMemberId)
        ORDER BY e.expense_date DESC, e.created_at DESC
        LIMIT :limit OFFSET :offset
    ]]></query>

    <query name="expense.countByUserAndPeriod"><![CDATA[
        SELECT COUNT(1)
        FROM expenses e
        WHERE e.user_id = :userId
          AND e.expense_date >= :startDate
          AND e.expense_date < :endDate
          AND (:categoryId IS NULL OR e.category_id = :categoryId)
          AND (:familyMemberId IS NULL OR e.family_member_id = :familyMemberId)
    ]]></query>

    <query name="expense.sumByUserAndPeriod"><![CDATA[
        SELECT COALESCE(SUM(e.amount), 0)
        FROM expenses e
        WHERE e.user_id = :userId
          AND e.expense_date >= :startDate
          AND e.expense_date < :endDate
          AND (:categoryId IS NULL OR e.category_id = :categoryId)
          AND (:familyMemberId IS NULL OR e.family_member_id = :familyMemberId)
    ]]></query>

    <query name="expense.update"><![CDATA[
        UPDATE expenses
        SET family_member_id = :familyMemberId,
            category_id = :categoryId,
            description = :description,
            amount = :amount,
            expense_date = :expenseDate,
            is_fixed = :isFixed,
            payment_method = :paymentMethod,
            notes = :notes,
            scope = :scope,
            updated_at = :updatedAt
        WHERE id = :id
          AND user_id = :userId
        RETURNING id,
                  user_id,
                  family_member_id,
                  category_id,
                  installment_group_id,
                  description,
                  amount,
                  expense_date,
                  installment_number,
                  total_installments,
                  is_recurring,
                  is_fixed,
                  payment_method,
                  notes,
                  scope,
                  created_at,
                  updated_at
    ]]></query>

    <query name="expense.delete"><![CDATA[
        DELETE FROM expenses
        WHERE id = :id
          AND user_id = :userId
    ]]></query>

    <query name="installmentGroup.insert"><![CDATA[
        INSERT INTO installment_groups (
            id,
            user_id,
            description,
            total_amount,
            total_installments,
            first_due_date,
            created_at
        )
        VALUES (
            :id,
            :userId,
            :description,
            :totalAmount,
            :totalInstallments,
            :firstDueDate,
            :createdAt
        )
        RETURNING id
    ]]></query>

    <query name="installmentGroup.findUserIdById"><![CDATA[
        SELECT user_id
        FROM installment_groups
        WHERE id = :id
        LIMIT 1
    ]]></query>

    <query name="installmentGroup.deleteExpenses"><![CDATA[
        DELETE FROM expenses
        WHERE installment_group_id = :id
          AND user_id = :userId
    ]]></query>

    <query name="installmentGroup.delete"><![CDATA[
        DELETE FROM installment_groups
        WHERE id = :id
          AND user_id = :userId
    ]]></query>

    <query name="installmentReport.openItems"><![CDATA[
        SELECT ig.id AS group_id,
               ig.description,
               ig.total_amount,
               ig.total_installments,
               e.id AS installment_id,
               e.installment_number,
               e.amount,
               e.expense_date AS due_date,
               future_summary.next_due_date,
               future_summary.future_total,
               future_summary.future_installments_count
        FROM installment_groups ig
        INNER JOIN (
            SELECT installment_group_id,
                   MIN(expense_date) AS next_due_date,
                   COALESCE(SUM(amount), 0) AS future_total,
                   COUNT(1) AS future_installments_count
            FROM expenses
            WHERE user_id = :userId
              AND installment_group_id IS NOT NULL
              AND expense_date >= :today
            GROUP BY installment_group_id
        ) future_summary ON future_summary.installment_group_id = ig.id
        INNER JOIN expenses e ON e.installment_group_id = ig.id
            AND e.user_id = ig.user_id
            AND e.expense_date >= :today
        WHERE ig.user_id = :userId
        ORDER BY future_summary.next_due_date ASC,
                 ig.description ASC,
                 e.expense_date ASC,
                 e.installment_number ASC
    ]]></query>

    <query name="expense.familyMemberBelongsToUser"><![CDATA[
        SELECT COUNT(1)
        FROM family_members
        WHERE id = :familyMemberId
          AND user_id = :userId
    ]]></query>

    <query name="expense.categoryIsAccessible"><![CDATA[
        SELECT COUNT(1)
        FROM expense_categories
        WHERE id = :categoryId
          AND (is_default = true OR user_id = :userId)
    ]]></query>

    <query name="budget.findById"><![CDATA[
        SELECT id,
               user_id,
               category_id,
               month,
               year,
               amount_limit,
               created_at
        FROM budgets
        WHERE id = :id
        LIMIT 1
    ]]></query>

    <query name="budget.findByUserAndPeriod"><![CDATA[
        SELECT id,
               user_id,
               category_id,
               month,
               year,
               amount_limit,
               created_at
        FROM budgets
        WHERE user_id = :userId
          AND month = :month
          AND year = :year
        ORDER BY created_at ASC
    ]]></query>

    <query name="budget.upsert"><![CDATA[
        INSERT INTO budgets (
            id,
            user_id,
            category_id,
            month,
            year,
            amount_limit,
            created_at
        )
        VALUES (
            gen_random_uuid(),
            :userId,
            :categoryId,
            :month,
            :year,
            :amountLimit,
            NOW()
        )
        ON CONFLICT (user_id, category_id, month, year)
        DO UPDATE SET amount_limit = EXCLUDED.amount_limit
        RETURNING id,
                  user_id,
                  category_id,
                  month,
                  year,
                  amount_limit,
                  created_at
    ]]></query>

    <query name="budget.update"><![CDATA[
        UPDATE budgets
        SET category_id = :categoryId,
            month = :month,
            year = :year,
            amount_limit = :amountLimit
        WHERE id = :id
          AND user_id = :userId
        RETURNING id,
                  user_id,
                  category_id,
                  month,
                  year,
                  amount_limit,
                  created_at
    ]]></query>

    <query name="budget.delete"><![CDATA[
        DELETE FROM budgets
        WHERE id = :id
          AND user_id = :userId
    ]]></query>

    <query name="budget.categoryIsAccessible"><![CDATA[
        SELECT COUNT(1)
        FROM expense_categories
        WHERE id = :categoryId
          AND (is_default = true OR user_id = :userId)
    ]]></query>

    <query name="budget.status"><![CDATA[
        SELECT c.id AS category_id,
               c.name AS category_name,
               c.icon AS category_icon,
               c.color_hex AS category_color_hex,
               b.id AS budget_id,
               b.amount_limit,
               COALESCE(SUM(e.amount), 0) AS spent_amount
        FROM expense_categories c
        LEFT JOIN budgets b ON b.category_id = c.id
            AND b.user_id = :userId
            AND b.month = :month
            AND b.year = :year
        LEFT JOIN expenses e ON e.category_id = c.id
            AND e.user_id = :userId
            AND e.expense_date >= :startDate
            AND e.expense_date < :endDate
        WHERE c.is_default = true
           OR c.user_id = :userId
        GROUP BY c.id, c.name, c.icon, c.color_hex, b.id, b.amount_limit
        ORDER BY c.name ASC
    ]]></query>

    <query name="budget.copyPreviousMonth"><![CDATA[
        INSERT INTO budgets (
            id,
            user_id,
            category_id,
            month,
            year,
            amount_limit,
            created_at
        )
        SELECT gen_random_uuid(),
               user_id,
               category_id,
               :toMonth,
               :toYear,
               amount_limit,
               NOW()
        FROM budgets
        WHERE user_id = :userId
          AND month = :fromMonth
          AND year = :fromYear
        ON CONFLICT (user_id, category_id, month, year)
        DO UPDATE SET amount_limit = EXCLUDED.amount_limit
    ]]></query>

    <query name="monthlyReport.incomeItems"><![CDATA[
        SELECT i.id,
               i.description,
               i.amount,
               i.income_date,
               i.category_id,
               i.is_recurring,
               c.name AS category_name,
               c.icon AS category_icon
        FROM incomes i
        LEFT JOIN expense_categories c ON c.id = i.category_id
        WHERE i.user_id = :userId
          AND i.income_date >= :startDate
          AND i.income_date < :endDate
        ORDER BY i.income_date DESC, i.created_at DESC
    ]]></query>

    <query name="monthlyReport.incomeTotal"><![CDATA[
        SELECT COALESCE(SUM(amount), 0)
        FROM incomes
        WHERE user_id = :userId
          AND income_date >= :startDate
          AND income_date < :endDate
    ]]></query>

    <query name="monthlyReport.ownerExpensesByCategory"><![CDATA[
        SELECT e.category_id,
               COALESCE(c.name, 'Sem categoria') AS category_name,
               c.icon AS category_icon,
               c.color_hex AS category_color_hex,
               COALESCE(SUM(e.amount), 0) AS total
        FROM expenses e
        LEFT JOIN expense_categories c ON c.id = e.category_id
        WHERE e.user_id = :userId
          AND e.scope = 'owner'
          AND e.expense_date >= :startDate
          AND e.expense_date < :endDate
        GROUP BY e.category_id, c.name, c.icon, c.color_hex
        ORDER BY total DESC, category_name ASC
    ]]></query>

    <query name="monthlyReport.ownerExpensesTotal"><![CDATA[
        SELECT COALESCE(SUM(amount), 0)
        FROM expenses
        WHERE user_id = :userId
          AND scope = 'owner'
          AND expense_date >= :startDate
          AND expense_date < :endDate
    ]]></query>

    <query name="monthlyReport.familyExpensesByMember"><![CDATA[
        SELECT e.family_member_id,
               COALESCE(fm.name, 'Familia') AS family_member_name,
               fm.relation AS family_member_relation,
               COALESCE(SUM(e.amount), 0) AS total
        FROM expenses e
        LEFT JOIN family_members fm ON fm.id = e.family_member_id
        WHERE e.user_id = :userId
          AND e.scope = 'family'
          AND e.expense_date >= :startDate
          AND e.expense_date < :endDate
        GROUP BY e.family_member_id, fm.name, fm.relation
        ORDER BY total DESC, family_member_name ASC
    ]]></query>

    <query name="monthlyReport.familyExpensesTotal"><![CDATA[
        SELECT COALESCE(SUM(amount), 0)
        FROM expenses
        WHERE user_id = :userId
          AND scope = 'family'
          AND expense_date >= :startDate
          AND expense_date < :endDate
    ]]></query>

    <query name="yearlyReport.incomeByMonth"><![CDATA[
        SELECT CAST(EXTRACT(MONTH FROM income_date) AS integer) AS month,
               COALESCE(SUM(amount), 0) AS total
        FROM incomes
        WHERE user_id = :userId
          AND income_date >= :startDate
          AND income_date < :endDate
        GROUP BY 1
        ORDER BY month ASC
    ]]></query>

    <query name="yearlyReport.ownerExpensesByMonth"><![CDATA[
        SELECT CAST(EXTRACT(MONTH FROM expense_date) AS integer) AS month,
               COALESCE(SUM(amount), 0) AS total
        FROM expenses
        WHERE user_id = :userId
          AND scope = 'owner'
          AND expense_date >= :startDate
          AND expense_date < :endDate
        GROUP BY 1
        ORDER BY month ASC
    ]]></query>

    <query name="yearlyReport.familyExpensesByMonth"><![CDATA[
        SELECT CAST(EXTRACT(MONTH FROM expense_date) AS integer) AS month,
               COALESCE(SUM(amount), 0) AS total
        FROM expenses
        WHERE user_id = :userId
          AND scope = 'family'
          AND expense_date >= :startDate
          AND expense_date < :endDate
        GROUP BY 1
        ORDER BY month ASC
    ]]></query>

    <query name="income.findByUserAndPeriod"><![CDATA[
        SELECT id,
               user_id,
               category_id,
               description,
               amount,
               income_date,
               is_recurring,
               notes,
               created_at,
               updated_at
        FROM incomes
        WHERE user_id = :userId
          AND income_date >= :startDate
          AND income_date < :endDate
        ORDER BY income_date DESC, created_at DESC
    ]]></query>

    <query name="income.findById"><![CDATA[
        SELECT id,
               user_id,
               category_id,
               description,
               amount,
               income_date,
               is_recurring,
               notes,
               created_at,
               updated_at
        FROM incomes
        WHERE id = :id
        LIMIT 1
    ]]></query>

    <query name="income.insert"><![CDATA[
        INSERT INTO incomes (
            id,
            user_id,
            category_id,
            description,
            amount,
            income_date,
            is_recurring,
            notes,
            created_at,
            updated_at
        )
        VALUES (
            :id,
            :userId,
            :categoryId,
            :description,
            :amount,
            :incomeDate,
            :isRecurring,
            :notes,
            :createdAt,
            :updatedAt
        )
        RETURNING id,
                  user_id,
                  category_id,
                  description,
                  amount,
                  income_date,
                  is_recurring,
                  notes,
                  created_at,
                  updated_at
    ]]></query>

    <query name="income.update"><![CDATA[
        UPDATE incomes
        SET category_id = :categoryId,
            description = :description,
            amount = :amount,
            income_date = :incomeDate,
            is_recurring = :isRecurring,
            notes = :notes,
            updated_at = :updatedAt
        WHERE id = :id
          AND user_id = :userId
        RETURNING id,
                  user_id,
                  category_id,
                  description,
                  amount,
                  income_date,
                  is_recurring,
                  notes,
                  created_at,
                  updated_at
    ]]></query>

    <query name="income.delete"><![CDATA[
        DELETE FROM incomes
        WHERE id = :id
          AND user_id = :userId
    ]]></query>

    <query name="income.categoryIsAccessible"><![CDATA[
        SELECT COUNT(1)
        FROM expense_categories
        WHERE id = :categoryId
          AND (is_default = true OR user_id = :userId)
    ]]></query>
    <query name="shareToken.revokeActiveFor"><![CDATA[
        UPDATE share_tokens
        SET revoked_at = :revokedAt
        WHERE user_id = :userId
          AND family_member_id = :familyMemberId
          AND month = :month
          AND year = :year
          AND revoked_at IS NULL
    ]]></query>

    <query name="shareToken.insert"><![CDATA[
        INSERT INTO share_tokens (
            id,
            user_id,
            family_member_id,
            month,
            year,
            token_hash,
            expires_at,
            created_at
        )
        VALUES (
            :id,
            :userId,
            :familyMemberId,
            :month,
            :year,
            :tokenHash,
            :expiresAt,
            :createdAt
        )
        RETURNING id,
                  user_id,
                  family_member_id,
                  NULL AS family_member_name,
                  NULL AS family_member_relation,
                  month,
                  year,
                  token_hash,
                  expires_at,
                  revoked_at,
                  created_at
    ]]></query>

    <query name="shareToken.findByUserId"><![CDATA[
        SELECT st.id,
               st.user_id,
               st.family_member_id,
               fm.name AS family_member_name,
               fm.relation AS family_member_relation,
               st.month,
               st.year,
               st.token_hash,
               st.expires_at,
               st.revoked_at,
               st.created_at
        FROM share_tokens st
        INNER JOIN family_members fm ON fm.id = st.family_member_id
        WHERE st.user_id = :userId
        ORDER BY st.created_at DESC
    ]]></query>

    <query name="shareToken.findById"><![CDATA[
        SELECT st.id,
               st.user_id,
               st.family_member_id,
               fm.name AS family_member_name,
               fm.relation AS family_member_relation,
               st.month,
               st.year,
               st.token_hash,
               st.expires_at,
               st.revoked_at,
               st.created_at
        FROM share_tokens st
        INNER JOIN family_members fm ON fm.id = st.family_member_id
        WHERE st.id = :id
        LIMIT 1
    ]]></query>

    <query name="shareToken.revokeById"><![CDATA[
        UPDATE share_tokens
        SET revoked_at = :revokedAt
        WHERE id = :id
          AND user_id = :userId
          AND revoked_at IS NULL
    ]]></query>

    <query name="shareToken.findValidByHash"><![CDATA[
        SELECT st.id,
               st.user_id,
               st.family_member_id,
               fm.name AS family_member_name,
               fm.relation AS family_member_relation,
               st.month,
               st.year,
               st.token_hash,
               st.expires_at,
               st.revoked_at,
               st.created_at
        FROM share_tokens st
        INNER JOIN family_members fm ON fm.id = st.family_member_id
        WHERE st.token_hash = :tokenHash
          AND st.revoked_at IS NULL
          AND st.expires_at > :now
        LIMIT 1
    ]]></query>

    <query name="shareToken.publicExpenses"><![CDATA[
        SELECT e.description,
               e.amount,
               e.expense_date,
               e.category_id,
               c.name AS category_name,
               c.icon AS category_icon,
               c.color_hex AS category_color_hex,
               e.payment_method,
               e.is_fixed
        FROM expenses e
        LEFT JOIN expense_categories c ON c.id = e.category_id
        WHERE e.user_id = :userId
          AND e.family_member_id = :familyMemberId
          AND e.scope = 'family'
          AND e.expense_date >= :startDate
          AND e.expense_date < :endDate
        ORDER BY e.expense_date DESC, e.created_at DESC
    ]]></query>

    <query name="shareToken.familyMemberBelongsToUser"><![CDATA[
        SELECT COUNT(1)
        FROM family_members
        WHERE id = :familyMemberId
          AND user_id = :userId
          AND active = true
    ]]></query>
</queries>
